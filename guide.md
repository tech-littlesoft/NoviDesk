---
layout: default
title: 用户操作指南 · User Guide
permalink: /guide/
toc: true
---

# 用户操作指南 <span class="en">User Guide · Get Started in 5 Minutes</span>

<p class="lead">5 分钟把 NoviDesk 用到最顺手——从安装到玩转分屏、时光收纳，一步步带你走完。</p>

## 1. 安装与首次启动 <span class="en">Install &amp; First Launch</span>

<p class="zh">从 GitHub Releases 下载最新安装包，双击按提示完成安装。首次启动时，应用会以默认设置出现在屏幕左下角，整个真实桌面保持干净。</p>
<p class="en">Grab the latest installer from GitHub Releases and follow the prompts. On first launch, the workspace appears at the bottom-left corner — your real desktop stays clean.</p>

## 2. 设置你的触发角 <span class="en">Pick Your Hot Corner</span>

<p class="zh">默认是屏幕左下角。如果你换了手或多显示器，可以在主菜单 → 触发角里改。每个屏幕的每个角都可单独启用——挑一个最不影响你日常操作的角落。</p>
<p class="en">Bottom-left is the default. Switch it from the main menu; every corner on every monitor can be enabled independently — pick one that never interferes with your flow.</p>

## 3. 把文件夹拖进卡片 <span class="en">Drag Folders into Cards</span>

<p class="zh">用资源管理器把常用文件夹直接拖到任意卡片上，松手即绑定。也可以拖到设置卡，再切换"单文件夹 / 多文件夹聚合"。</p>
<p class="en">Drag any folder from Explorer onto a card to bind it. Or drop onto the Settings card and toggle single-folder / multi-folder aggregation.</p>

## 4. 调整卡片布局 <span class="en">Rearrange Cards</span>

<p class="zh">按住卡片<b>标题栏</b>拖到另一个卡片上，两者互换位置——10 秒搭出你的专属版面。</p>
<p class="en">Drag a card's title bar onto another card to swap them — build your layout in seconds.</p>

## 5. 一秒进入分屏 <span class="en">Instant Split View</span>

<p class="zh">在文件夹<b>名称</b>上双击（不是图标），进入左右分屏：左侧导航、右侧预览；点左侧任意空白处即可退出。</p>
<p class="en">Double-click a folder name (not the icon) for a side-by-side split: navigate left, preview right; click the left pane to exit.</p>

## 6. 时光收纳 <span class="en">TimeCollect</span>

<p class="zh">新文件会按"今天 / 昨天 / 前天 / 近七天 / 早些时候"自动归集，实时刷新。再也不用手动建日期文件夹。</p>
<p class="en">New files auto-bucket into Today / Yesterday / 2 days ago / Last 7 days / Earlier — refreshed live.</p>

## 7. 切换主题与视图 <span class="en">Themes &amp; View Modes</span>

<p class="zh">主菜单 → 主题 一键切换深 / 浅，并自动记住你的选择。配合大卡片 / 小卡片视图，亮屏护眼随心。</p>
<p class="en">One tap in the main menu toggles dark/light and remembers your choice; pair it with large/small card view.</p>

## 8. 进阶玩法 <span class="en">Pro Tips</span>

<ul class="tips">
  <li>
    <b>多触发角联动</b>
    <p class="zh">每个显示器单独设角，多屏工作流不打架。</p>
    <p class="en">Set a corner per monitor — multi-screen workflows stay clean.</p>
  </li>
  <li>
    <b>跨面板拖拽 = 移动</b>
    <p class="zh">把文件从 A 卡拖到 B 卡 = 移动；拖到空白卡片 = 复制。</p>
    <p class="en">Drag from card A to B = move; drag into an empty card = copy.</p>
  </li>
  <li>
    <b>原生右键菜单</b>
    <p class="zh">任意文件右键 = 系统 Explorer 菜单，所有 Shell 操作一键可达。</p>
    <p class="en">Right-click any file for the native Explorer menu — every shell action, one gesture away.</p>
  </li>
  <li>
    <b>离线激活 Pro</b>
    <p class="zh">Pro 通过离线激活码开启，不连接任何服务器，详见 <a href="{{ '/buy/' | relative_url }}">升级 Pro</a>。</p>
    <p class="en">Pro unlocks via offline activation code — no server contact. See <a href="{{ '/buy/' | relative_url }}">Upgrade to Pro</a>.</p>
  </li>
</ul>

<p class="en">See the <a href="{{ '/' | relative_url }}">Overview</a> for the full feature walkthrough, or jump to <a href="{{ '/buy/' | relative_url }}">Upgrade to Pro</a>.</p>